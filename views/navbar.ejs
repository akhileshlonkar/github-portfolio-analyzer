<nav class="navbar navbar-expand-lg sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/profile">
      <i class="fa-brands fa-github"></i> Portfolio Analyzer
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      style="border-color: var(--border-color);">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" href="/profile">Dashboard</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/profile/new">Analyze</a>
        </li>
      </ul>

      <% if (!currentUser) { %>
        <a href="/login" class="btn btn-outline-dark me-2">Sign in</a>
        <a href="/signup" class="btn btn-dark">Sign up</a>
      <% } %>
      <% if (currentUser) { %>
        <span class="navbar-text me-3">
          <i class="fa-solid fa-user"></i> <%= currentUser.username %>
        </span>
        <a href="/logout" class="btn btn-outline-danger">Logout</a>
      <% } %>
    </div>
  </div>
</nav>
